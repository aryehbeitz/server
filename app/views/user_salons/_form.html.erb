<%= form_with(model: user_salon, local: true) do |form| %>
  <% if user_salon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_salon.errors.count, "error") %> prohibited this user_salon from being saved:</h2>

      <ul>
      <% user_salon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :guest_amount %>
    <%= form.text_field :guest_amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
